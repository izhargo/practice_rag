# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`practice_rag` is a Python project for practicing Retrieval-Augmented Generation (RAG) implementations. The repository is in early development — no source code or dependencies exist yet.

## Setup

No build or dependency files have been added yet. Once they are, update this file with the relevant commands (e.g., `pip install -r requirements.txt`, `poetry install`, `uv sync`).

## Remote

`git@github.com:izhargo/practice_rag.git`
